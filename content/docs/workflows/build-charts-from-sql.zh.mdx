---
title: Build Charts from SQL
description: 把查询结果整理成适合可视化的输出结构。
---

# Build Charts from SQL

最好的图表通常来自已经结构清晰、表达意图明确的查询结果。

## 结果准备建议

- 使用稳定且明确的标签。
- 每一列尽量只表达一种指标含义。
- 如果图表依赖顺序，直接在 SQL 中排序。
- 能先聚合的场景尽量先聚合。